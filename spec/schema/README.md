# Schema (placeholder)

This directory will contain the canonical entity/component schema.

Planned files:
- `entities.json` / `components.json` (or protobuf/ts definitions)
- `versioning.md`

Principles:
- Schema-first development
- Backward-compatible evolution where possible
- Clear migrations for breaking changes
